---
title: "Installing RHOAI Nightly Builds"
subtitle: "Via OLM Manual Way & Through Claude Code Skills"
author: "Karishma Punwatkar & Shelton Cyril"
format:
  revealjs:
    theme: dark
    transition: slide
    slide-number: true
    footer: "RHOAI Nightly Installation"
    width: 1920
    height: 1080
    css: styles.css
    title-slide-attributes:
      data-background-gradient: "linear-gradient(135deg, #0d1b2a 0%, #1b2838 25%, #162447 50%, #1f4068 75%, #1b2838 100%)"
---

## Agenda

- What is RHOAI & Why Nightly Builds?
- Prerequisites
- Manual Installation via OLM (with demos)
- Verifying Deployment & TrustyAI Stability
- Automated Installation via Claude Code Skills
- Cleanup
- Q&A

## What is RHOAI?

- **Red Hat OpenShift AI** — ML/AI platform on OpenShift
- Components: Dashboard, KServe, TrustyAI, Model Registry, Workbenches
- Managed via **DataScienceCluster (DSC)** custom resource
- Operators installed and managed through **OLM** (Operator Lifecycle Manager)

## Why Nightly Builds?

- Test latest features **before GA release**
- Validate changes early in the development cycle
- Catch integration issues between components before release
- Verify our TrustyAI changes work within the full RHOAI stack

## Prerequisites

- OpenShift cluster with **cluster-admin** access
- `oc` CLI installed and logged in
- Clone the install repo:

```bash
git clone https://gitlab.cee.redhat.com/data-hub/olminstall
cd olminstall
```

- Nightly catalog image URL from the build system

## Architecture Overview

::: {style="font-size: 0.8em; line-height: 1.8; text-align: center;"}

[openshift-marketplace<br>CatalogSource (rhoai-catalog-dev)]{style="display:inline-block; padding: 15px 40px; background-color: #1a5276; color: white; border-radius: 8px; font-size: 1.2em; margin: 10px;"}

⬇

[redhat-ods-operator<br>Subscription → InstallPlan → CSV → Operator]{style="display:inline-block; padding: 15px 40px; background-color: #7d3c98; color: white; border-radius: 8px; font-size: 1.2em; margin: 10px;"}

⬇

[redhat-ods-applications<br>DataScienceCluster → Components<br>(Dashboard, KServe, TrustyAI, ModelRegistry, Workbenches)]{style="display:inline-block; padding: 15px 40px; background-color: #1e8449; color: white; border-radius: 8px; font-size: 1.2em; margin: 10px;"}

:::

## Step 1 — Get the Nightly Image

The `rhoai-devops-bot` posts nightly build images to Slack channel `#rhoai-build-notifications`:

**Slack Channel:**  `#rhoai-build-notifications`

**Bot Message:**

> A new Nightly Build is available for RHOAI v3.5.0
>
> Image: `quay.io/rhoai/rhoai-fbc-fragment:rhoai-<release-version>@sha256:<digest>`
>
> Commit: `a921bd2`

Copy the **Image** URL for the next step.

## Step 1 — Configure CatalogSource

```bash
./setup.sh -t operator -u stable-<release-version> \
  -i quay.io/rhoai/rhoai-fbc-fragment:rhoai-<release-version>@sha256:<digest>
```

**Output:**

```{style="font-size: 0.75em;"}
namespace/redhat-ods-operator created
catalogsource.operators.coreos.com/rhoai-catalog-dev created
operatorgroup.operators.coreos.com/rhoai-operator-dev created
subscription.operators.coreos.com/rhoai-operator-dev created
```

## Step 1 — Verify Operator in Web Console

**Navigate to:**

- Operators → Installed Operators
- Namespace: `redhat-ods-operator`
- Verify CSV status: **Succeeded**

## Step 1 — Web Console Demo

{{< video demos/02-operator-console.mp4 height="1000" >}}

## Step 2 — Create DataScienceCluster & Verify {style="font-size: 0.9em;"}

- Create DSC via **Web Console**: Operators → Installed Operators → RHOAI → Data Science Cluster → Create
- Or via CLI: `./create-dsc.sh`
- Verify: DSC reaches **Ready** phase, all pods in `redhat-ods-applications` are **Running/Completed**

![](images/dsc-ready-console.png){.r-stretch fig-align="center"}

## Step 3 — Verify TrustyAI

![](images/trustyai-verify-terminal.png){.r-stretch fig-align="center"}

**TrustyAI is stable when:** `TrustyAIReady: True`, CRDs exist, and pod is **Running**

## TrustyAI Verification Commands

```{.bash style="font-size: 1.0em;"}
# Check DSC component conditions
oc get datasciencecluster -o jsonpath='{.items[0].status.conditions}' | jq .

# Verify TrustyAI CRDs are installed
oc get crd | grep trustyai

# Check TrustyAI operator pod is running
oc get pods -n redhat-ods-applications | grep trustyai

# Check DSC and DSCI status
oc get datasciencecluster -A
oc get dsci -A
```

## Cleanup

```{.bash style="font-size: 1.0em;"}
# Graceful cleanup
./cleanup.sh -t operator -g

# Complete cleanup with 19-point verification
./complete-cleanup.sh
```

**Handles:** stuck namespaces, CRDs, orphaned webhooks, operator resources

<!-- {{< video demos/07-cleanup.mp4 >}} -->

## Common Issues

| Problem | Solution |
|---------|----------|
| Pods stuck in `Pending` | Check node resources |
| CSV not `Succeeded` | Check InstallPlan approval |
| Namespace stuck `Terminating` | `./remove-stuck-crds.sh` |
| CatalogSource not ready | Check image pull access |

## TrustyAI: DSC Stuck in DeploymentsNotReady

![](images/trustyai-not-ready.png){.r-stretch fig-align="center"}

## TrustyAI DeploymentsNotReady — Cause & Fix

**Symptom:** DSC shows `DeploymentsNotReady` for TrustyAI even though pods are running

**Root cause:** Image pull secret or ServiceAccount gets regenerated, causing a transient pod failure. Pod self-heals, but `rhods-operator` never re-reconciles — DSC shows stale status.

**Fix:**

```bash
oc annotate trustyai default-trustyai \
  opendatahub.io/manual-reconcile=$(date +%s) --overwrite
```

Forces a fresh reconcile → controller sees healthy deployment → flips `TrustyAIReady: True`

## Subscription Channel Mismatch

**Symptom:** Subscription fails to resolve — no InstallPlan created, operator never installs

**Root cause:** The catalog image (e.g., a 3.6 EA build) doesn't publish the channel you specified (e.g., `stable-3.6`). EA/nightly builds often only exist in the `beta` channel.

**Fix:**

```bash
# Patch the subscription to the correct channel
oc patch subscription rhoai-operator-dev -n redhat-ods-operator \
  --type merge -p '{"spec":{"channel":"beta"}}'
```

## Channel Mismatch — Prevention

**Use the correct `-u` flag when running setup:**

```bash
# For EA/nightly builds, use beta channel
./setup.sh -t operator -u beta -i <image-url>
```

**Check available channels before installing:**

```bash
oc get packagemanifest rhoai -n openshift-marketplace \
  -o jsonpath='{.status.channels[*].name}'
```

## ROSA HCP: Pull Secret Workaround

**Problem:** On ROSA-hosted clusters, the global pull-secret in `openshift-config` gets **immediately rewritten** back to original ([OCPBUGS-23901](https://issues.redhat.com/browse/OCPBUGS-23901))

**Why it matters:** Nightly builds require pull access to `brew.registry.redhat.io` or `quay.io/rhoai` — without the pull secret, image pulls fail

**Solution:** Use **Kyverno** to inject pull secrets into every pod

## ROSA HCP: Workaround Steps

1. Create a secret `pull-secret-brew` in `openshift-config` with your registry credentials
2. Deploy Kyverno to auto-sync the secret to **all namespaces**
3. Kyverno policy adds the secret to every pod's `imagePullSecrets`

**Prerequisites:** `jq`, `yq`, and `~/.docker/config.json` with credentials

**Full steps:** [ROSA Pull Secret Workaround Doc](https://docs.google.com/document/d/12FoMt1_djxEkhuAsRjU40aIxnlo-0SATK-E4qihYQdQ)

## Q&A

**Resources:**

- Install repo: [gitlab.cee.redhat.com/data-hub/olminstall](https://gitlab.cee.redhat.com/data-hub/olminstall)
- Cleanup guide: `COMPLETE-CLEANUP-README.md`
- ROSA pull secret workaround: [Google Doc](https://docs.google.com/document/d/12FoMt1_djxEkhuAsRjU40aIxnlo-0SATK-E4qihYQdQ)

Thank you!
